---
id: "sec-10"
numero: 9
titulo: "Modelado de comportamiento con máquina de estados"
slug: "10-maquina-estados"
resumen: "Una máquina de estados modela el ciclo de vida de un objeto reactivo según estados, eventos, guardas y efectos."
keywords: ["máquina de estados", "estado", "transición", "evento", "guarda", "efecto", "objeto reactivo"]
relacionadas: ["03-orientacion-objetos", "11-secuencia-comunicacion"]
---

### 9.1. Qué es un diagrama de máquina de estados

<Definicion termino="diagrama de máquina de estados">
Un <strong>diagrama de máquina de estados</strong> muestra la secuencia de estados por los que pasa un objeto, los eventos que provocan transiciones, las condiciones que las habilitan y las acciones que ocurren.
</Definicion>

Se usa para modelar comportamiento dinámico de elementos cuyos objetos cambian su comportamiento según el estado.

### 9.2. Clasificación y uso

Es un diagrama de comportamiento, dinámico y lógico. Sirve para explorar clases, actores, subsistemas o componentes con ciclos de vida claros, especialmente en sistemas de tiempo real o entidades reactivas.

### 9.3. Elementos principales

- <strong>Estado inicial</strong> y <strong>estado final</strong>.
- <strong>Estado:</strong> condición estable durante un período identificable.
- <strong>Transición:</strong> cambio desde un estado origen hacia un destino.
- <strong>Evento disparador:</strong> hecho que activa una transición.
- <strong>Condición de guarda:</strong> expresión booleana requerida.
- <strong>Efecto:</strong> acción ejecutada al dispararse la transición.

<MermaidDiagram caption="Elementos de una transición" code={`flowchart LR
  O[Estado origen] -->|"evento [guarda] / efecto"| D[Estado destino]`} />

#### 9.3.1. Anatomía completa de un estado

Un estado no es solo un nombre: UML permite adornarlo con acciones internas. Los resúmenes teóricos describen estas partes (aunque en los parciales de análisis casi no se usen).

<TablaComparativa
  caption="Partes internas de un estado (vocabulario UML)"
  columnas={["Parte", "Qué significa", "Notación"]}
  filas={[
    ["<strong>Efecto de entrada</strong>", "Acción que se ejecuta siempre al <em>entrar</em> al estado.", "entry / accion()"],
    ["<strong>Efecto de salida</strong>", "Acción que se ejecuta siempre al <em>salir</em> del estado.", "exit / accion()"],
    ["<strong>Actividad <code>do</code></strong>", "Ejecución en curso, no atómica, mientras se está en el estado; se interrumpe si llega un evento que dispara una transición.", "do / actividad()"],
    ["<strong>Transición interna</strong>", "Responde a un evento sin salir del estado (no dispara entry/exit).", "evento / accion()"],
    ["<strong>Evento diferido</strong>", "Evento que no se atiende ahora: se encola y se maneja en otro estado.", "evento / defer"]
  ]}
/>

<MermaidDiagram caption="Estado con efectos de entrada, salida y actividad do" code={`stateDiagram-v2
  [*] --> Reproduciendo
  Reproduciendo : entry / encenderLuz()
  Reproduciendo : do / reproducirVideo()
  Reproduciendo : exit / apagarLuz()
  Reproduciendo --> Pausado : pausar()
  Pausado --> Reproduciendo : reanudar()`} />

<Callout tipo="atencion">
En los parciales de análisis la cátedra rara vez usa <code>entry/exit/do</code>: modela las acciones como <strong>efectos de transición</strong> (`/ metodo()`). Conviene reconocer la notación, pero al resolver seguí el estilo `Nº CU. Nombre [guarda] / metodo()`.
</Callout>

### 9.4. Sintaxis conceptual de una transición

<EjemploClase titulo="Sintaxis">
estado origen -- evento [condición] / efecto -- estado destino
</EjemploClase>

Ejemplo: <strong>Creada -- confirmarOrden() [hayContenedoresDisponibles] / asignarContenedores() -- Confirmada</strong>.

<TablaComparativa
  caption="Partes de la transición"
  columnas={["Parte", "Qué representa", "Cómo se nombra"]}
  filas={[
    ["Evento", "Lo que ocurre y dispara el cambio.", "Verbo en infinitivo o método: confirmar(), cancelar()."],
    ["Guarda", "Condición booleana que debe cumplirse.", "[hay stock], [datos completos]."],
    ["Efecto", "Acción ejecutada durante la transición.", "/ reservar(), / registrarCambio()."],
    ["Destino", "Nuevo estado estable del objeto.", "Confirmada, Cancelada, En traslado."]
  ]}
/>

<Callout tipo="tip">
En los ejercicios, los eventos de la máquina suelen transformarse en métodos de la clase de análisis. Por eso no alcanza con dibujar estados: las transiciones deben tener nombres operativos.
</Callout>

### 9.5. Objeto reactivo

Un objeto reactivo depende de eventos y del estado en que se encuentra. Para modelarlo se identifican estados relevantes, eventos, guardas, acciones, estado inicial y finales.

<EjemploReal titulo="Ciclo de vida del pedido en la pizzería" dominio="Caso cátedra">
El `Pedido` es un objeto reactivo clásico: lo que se le permite hacer depende de su estado.
<br />`PtePreparación` → (confirmar) → `EnPreparación` → (informar cierre) → `PteFacturación` → (generar factura) → `Facturado`. Desde casi cualquier estado puede ir a `Cancelado` (cancelar). La regla "solo se puede modificar mientras está *PtePreparación*" es exactamente comportamiento que cambia según el estado: por eso el `Pedido` merece máquina de estados y no un simple atributo de texto.
</EjemploReal>

<MermaidDiagram caption="OrdenDeTraslado como objeto reactivo" code={`stateDiagram-v2
  [*] --> Borrador
  Borrador --> Pendiente: confirmar() [datosCompletos] / reservarRecursos()
  Pendiente --> EnTransito: iniciarTraslado() [vehiculoAsignado] / registrarSalida()
  EnTransito --> Entregada: confirmarEntrega() / cerrarOrden()
  Borrador --> Cancelada: cancelar() / descartar()
  Pendiente --> Cancelada: cancelar() [sinSalida] / liberarRecursos()
  EnTransito --> Cancelada: cancelarPorIncidente() / registrarIncidente()
  Entregada --> [*]
  Cancelada --> [*]`} />

#### 9.5.1. Patrones de transición que usa la cátedra

En las soluciones de los prácticos aparecen tres patrones recurrentes que conviene tener listos para el parcial.

<strong>Transición a sí mismo con contador (reintentos).</strong> Una transición sale y vuelve al mismo estado mientras una condición se cumple; al agotarse, va a otro estado. En el caso <em>Agua/Medición</em>, importar reintenta y a los dos intentos fallidos pasa a `Erronea`. En <em>Pastelería/Pedido</em>, autorizar el cobro se reintenta `[intentos <= 2]`.

<MermaidDiagram caption="Self-transition con contador (Medición y Pedido)" code={`stateDiagram-v2
  [*] --> ImportadaConError
  ImportadaConError --> ImportadaConError: 17 Importar [1er intento fallido] / registrarMedicion()
  ImportadaConError --> Erronea: 17 Importar [2do intento fallido] / registrarMedicion()
  Confirmado --> Confirmado: 17 Autorizar cobro [quedan intentos] / autorizarCobro()
  Confirmado --> CobroRealizado: 17 Autorizar cobro [aceptada] / autorizarCobro()
  Confirmado --> CobroRechazado: 17 Autorizar cobro [rechazada o sin intentos] / autorizarCobro()`} />

<strong>Más de una transición inicial según una guarda.</strong> El pseudoestado inicial `[*]` puede llevar a distintos estados según una regla del enunciado. En <em>FIA F1</em>, una Orden de Traslado nace `Creada` si la genera el Jefe de Mecánica, pero `Aprobada` si la genera el Jefe de Equipo. En <em>Pastelería</em>, el pedido nace `EsperandoAceptación` o `EnAnálisisDestino` según si el destino es frecuente.

<MermaidDiagram caption="Transiciones iniciales con guarda (FIA F1)" code={`stateDiagram-v2
  [*] --> Creada: Generar OT [crea Jefe Mecánica] / new()
  [*] --> Aprobada: Generar OT [crea Jefe Equipo] / new()
  Creada --> Aprobada: Registrar evaluación / aprobar()
  Creada --> Rechazada: Registrar evaluación / rechazar()`} />

<strong>Estado compuesto con submáquina propia.</strong> Cuando una fase tiene subetapas, se la modela como estado compuesto y se la expande en un diagrama aparte. En <em>Pastelería</em>, `EnDespacho` se abre en `EntregadoACorreo → EnDistribución → EnReparto`.

<MermaidDiagram caption="Submáquina del estado EnDespacho (Pastelería)" code={`stateDiagram-v2
  [*] --> EntregadoACorreo: 27 Registrar despacho / despachar()
  EntregadoACorreo --> EnDistribucion: 25 Actualizar estado / enviarACentroDist()
  EnDistribucion --> EnReparto: 25 Actualizar estado / iniciarReparto()
  EnReparto --> [*]`} />

### 9.6. Buenas prácticas

- Elegir una clase cuyo comportamiento realmente dependa del estado.
- No modelar como estado algo que solo es un dato sin impacto.
- Nombrar estados como condiciones estables.
- Verificar que todos los estados sean alcanzables.
- Verificar que no existan estados muertos no deseados.
- Evitar usar casos de uso como estados.
- Agregar guardas cuando una regla de negocio decide entre dos caminos.
- Registrar cambios de estado cuando el enunciado pide historial o trazabilidad.

<MermaidDiagram caption="Checklist para decidir si corresponde máquina de estados" code={`flowchart TD
  A[¿La clase cambia de comportamiento según estado?] -->|No| B[No hacer máquina]
  A -->|Sí| C[¿Hay eventos que disparan cambios?]
  C -->|No| B
  C -->|Sí| D[Identificar estados estables]
  D --> E[Definir transiciones con evento, guarda y efecto]`} />

### 9.7. Cómo derivar métodos y reglas

Una máquina de estados no queda aislada: debe impactar en la clase modelada y en la vista de clases de análisis.

<MermaidDiagram caption="De la máquina a la clase de análisis" code={`flowchart LR
  Estado[Estados] --> Atributo[atributo estado o situacion]
  Evento[Eventos] --> Metodos[metodos de la clase]
  Guarda[Guardas] --> Reglas[responsabilidades de validacion]
  Efecto[Efectos] --> Operaciones[operaciones internas]
  Historial[Historial requerido] --> CambioEstado[clase CambioEstado]
  Atributo --> Clase[Clase de analisis]
  Metodos --> Clase
  Reglas --> Clase
  Operaciones --> Clase
  CambioEstado --> Clase`} />

<TablaComparativa
  caption="Qué mirar en el enunciado"
  columnas={["Señal en el texto", "Qué modelar"]}
  filas={[
    ["'pasa a', 'queda en', 'cambia a'", "Estado o transición."],
    ["'si', 'siempre que', 'cuando corresponda'", "Guarda o condición de control."],
    ["'se registra', 'se notifica', 'se libera'", "Efecto de transición o responsabilidad asociada."],
    ["'historial', 'fecha de cambio', 'responsable'", "Clase CambioEstado o relación similar."],
    ["'reanudar', 'suspender', 'volver al estado anterior'", "Estado suspendido y pseudoestado de historia."]
  ]}
/>

### 9.8. Pseudoestado de historia y estado suspendido

Uno de los PDFs menciona el caso de un estado suspendido que debe recordar a qué estado volver. Para eso se usa un pseudoestado de historia.

<MermaidDiagram caption="Historia: volver al estado previo luego de suspender" code={`stateDiagram-v2
  direction LR
  [*] --> EnCurso
  state EnCurso {
    [*] --> EnPreparacion
    EnPreparacion --> EnTraslado: iniciarTraslado()
    EnTraslado --> EsperandoConfirmacion: registrarLlegada()
    EsperandoConfirmacion --> [H]
  }
  EnCurso --> Suspendida: suspender()
  Suspendida --> EnCurso: reanudar() [usa H]
  EnCurso --> Cerrada: cerrar()
  Cerrada --> [*]`} />

<Callout tipo="atencion">
El pseudoestado de historia sirve cuando “reanudar” debe volver al subestado anterior, no a un inicio fijo.
</Callout>

#### 9.8.1. Subestados, concurrencia y submáquinas (vocabulario UML)

Los resúmenes teóricos describen formas avanzadas de estructurar una máquina. Conviene reconocerlas aunque el parcial de análisis casi siempre use solo las dos primeras.

<TablaComparativa
  caption="Tipos de subestados y mecanismos de composición"
  columnas={["Mecanismo", "Idea", "Cuándo aparece"]}
  filas={[
    ["<strong>Subestados no ortogonales</strong> (secuenciales/disjuntos)", "El objeto está en el compuesto y en <em>uno solo</em> de sus subestados a la vez.", "Lo habitual: una fase con etapas internas (EnDespacho)."],
    ["<strong>Subestados ortogonales</strong> (concurrentes)", "El compuesto se divide en regiones que se ejecutan <em>en paralelo</em>.", "Dos comportamientos simultáneos del mismo objeto."],
    ["<strong>Fork / Join</strong>", "Fork reparte el control a varias regiones; Join lo vuelve a unir cuando todas terminan.", "Entrada/salida de regiones ortogonales."],
    ["<strong>Submáquina</strong>", "Una máquina referenciada desde otra para reusar comportamiento.", "Modelos grandes que se descomponen."],
    ["<strong>Objetos activos</strong>", "Alternativa a las regiones: dos objetos, cada uno con su propia máquina.", "Cuando los flujos concurrentes se coordinan por mensajes, no por estado compartido."]
  ]}
/>

<MermaidDiagram caption="Regiones ortogonales (concurrentes) con fork conceptual" code={`stateDiagram-v2
  [*] --> Mantenimiento
  state Mantenimiento {
    [*] --> Probando
    Probando --> Diagnosticado: autodiagnóstico()
    --
    [*] --> Esperando
    Esperando --> Atendiendo: teclaPulsada()
  }
  Mantenimiento --> [*]`} />

<Callout tipo="atencion">
Convención de cátedra: el <strong>estado de historia (`H`)</strong> rara vez se dibuja en los parciales. La cátedra implementa la historia de forma <strong>estructural</strong> con una clase `CambioEstado...`: el <em>estado actual</em> es el cambio cuya `fechaHoraFin` está vacía (método `esEstadoActual()`), y para "volver al punto donde se interrumpió" se guarda un puntero `actual`/`anterior` (caso <em>Compras</em>, estado `EnCorrección`).
</Callout>

<MermaidDiagram caption="Historia estructural: clase CambioEstado en vez del símbolo H" code={`classDiagram
  class OrdenMedica {
    <<entity>>
    +obtenerEstadoActual()
    +cambiarEstado()
  }
  class CambioEstadoOM {
    <<entity>>
    -fechaHoraInicio
    -fechaHoraFin
    +esEstadoActual()
  }
  class Estado {
    <<entity>>
    -nombre
  }
  OrdenMedica "1" --> "1..*" CambioEstadoOM : actual / anterior
  CambioEstadoOM "*" --> "1" Estado`} />

### 9.9. Diferencia con diagramas de interacción

La máquina de estados se concentra en el ciclo de vida de un objeto o clase. Secuencia y comunicación muestran colaboración entre varios objetos durante un escenario.

<EjemploReal titulo="Estado de una reserva de vuelo" dominio="Cotidiano">
Otro objeto reactivo: una `Reserva` de pasaje. `Pendiente` → (pagar [hay asiento]) → `Confirmada` → (hacer check-in) → `EmitidaTarjetaEmbarque`. Si no pagás dentro del plazo: `Pendiente` → (vencerPlazo) → `Expirada`. Acá la guarda `[hay asiento]` decide si la confirmación procede: una regla de negocio que vive en la transición.
</EjemploReal>

<Callout tipo="error">
No uses máquina de estados para cualquier atributo llamado “estado”. Corresponde cuando ese estado cambia el comportamiento permitido del objeto.
</Callout>

<Callout tipo="info" titulo="¿Y si importa el tiempo?">
Los resúmenes mencionan el <strong>diagrama de Timing</strong>, que "cubre una falencia de la máquina de estados": muestra cómo cambian los estados o condiciones de uno o más objetos <em>a lo largo del tiempo</em>, con sus restricciones de duración. La máquina de estados dice <em>qué</em> estados hay y <em>por qué</em> se transita; el Timing agrega <em>cuándo</em> y por <em>cuánto</em>.
</Callout>
